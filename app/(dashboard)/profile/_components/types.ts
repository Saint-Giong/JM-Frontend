export type { Job } from '@/components/job';

export interface ProfileFormData {
  companyName: string;
  website: string;
  aboutUs: string;
  whoWeAreLookingFor: string;
  address: string;
  phone: string;
  email: string;
}
